//do not change this files content.
export const transitionDuration = 250;

export const searchDelay = 1000;

export const cacheDurations = {
  default: 1 /*in minutes*/,
};

export const fetchLimits = {
  default: 10,
  blogs: 10,
};

//toast
export const toastTransitionDuration = 250;
export const toastInterval = 750;
export const toastDuration = 5000;
export const toastAutoCloseWindowWidth = 720;
